import React from 'react';

// interface IProps extends React.PropsWithChildren  {
//   isOpen: boolean,
//   title: string,
//   user: IUser,
//   numbs: number[],
//   todos: ITodo[]
// }

// const Dashboard: React.FC<IProps> = ({ isOpen })
// const Dashboard = ({ isOpen, children, title, user, numbs, todos }: IProps)
const Dashboard: React.FC = () => {
  return (
    <>
      this is dashboard
    </>
  )
};

export default Dashboard;